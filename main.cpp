#include "WindowSetup.h"
#include <iostream>
#include <cstdio>

using namespace std;

int main (void)
{
  setupGameWindow();
  printf("WeirdChamp");
  int i; cin >> i; //This is just to keep the console open because nothing else keeps the game running
}